from packages.funcs import *

if __name__ == '__main__':

    nested_list = [['a', 'b', 'c'],
                   ['d', 'e', 'f', 'h', False],
                   [1, 2, None]]

    print(summator(2, 3))
    print(generator(nested_list))
